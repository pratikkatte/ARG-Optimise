# ARGFlow dependencies. Tested with Python 3.11 on Linux.
#
#   pip install -r requirements.txt
#
# For a CPU-only machine, install PyTorch first:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu

# Training, sampling and evaluation
numpy==2.4.6
torch==2.4.1
PyYAML==6.0.2
tskit==1.0.3
msprime==1.4.4
scipy==1.17.1
pandas==3.0.6
matplotlib==3.11.2
wandb==0.30.0

# ARGInfer baseline (also needed to read its saved samples during evaluation)
arginfer==0.0.1
bintrees==2.2.0
sortedcontainers==2.4.0
tqdm==4.70.1

# Tests and the POC supervisor
pytest==9.1.1
psutil==6.0.0
